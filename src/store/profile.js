const actions = {};

const getters = {};

const mutations = {};

const state = {
  name: "Unknown",
  accounts: [],
  transactions: [],
};

export default {
  namespaced: true,
  actions,
  getters,
  mutations,
  state,
};
