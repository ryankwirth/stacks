const actions = {
  onInstrument(test, test2) {
    console.log("dashboard/onInstrument");
    console.log(test);
    console.log(test2);
  },
};

const getters = {};

const mutations = {};

const state = {};

export default {
  namespaced: true,
  actions,
  getters,
  mutations,
  state,
};
